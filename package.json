{
  "name": "parkxigo",
  "version": "1.0.0",
  "description": "A modern parking application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd backend && npm run build && cp ../backend/dist-server.js ../server.js",
    "postbuild": "ls -la && ls -la backend/dist/"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react-leaflet": "^5.0.0",
    "express": "^4.21.2",
    "cors": "^2.8.5"
  }
}
